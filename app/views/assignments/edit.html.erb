<h3>editing existing article</h3>


<% if @assignment.errors.any? %>

<h3> the following errors prevent the article from getting created</h3>

   <ul> <% @assignment.errors.full_messages.each do |msg| %>
  <li> <%= msg %> </li>
   <% end %>
   </ul>
    <% end %>

<%= form_for @assignment do |g| %>

<p> 
<%= g.label :title %></br>
<%= g.text_field :title %>
</p>

<p> 

<%= g.label :description %></br>
<%= g.text_area :description %>
</p>

<p>
  <%= g.submit%>
</p>
  
<% end %>

<%= link_to "Back", assignments_path %>